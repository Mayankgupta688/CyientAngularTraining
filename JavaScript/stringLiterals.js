var salutation = "Hello";

var userName = "Mayank";

var question = "How are You ?" 

console.log(`${salutation} ${userName}!! ${question}`)

console.log(`${1 + 1} "Mayank"`)

console.log(`${Math.floor(Math.random() * 100)} "Mayank"`)

console.log(`Hello ${userName}...
    ${question}
    Lets Start with the Introduction...
`)

for(var i = 0; i<5; i++) {
    console.log(`Count is ${i}`)
}